image: moshedayan/gitpod:latest

tasks:
  - name: tmux
    command: /usr/bin/tmux -f .dotfiles/tmux/tmux.conf
    env:
      EDITOR: vim
      AWS_PROFILE: private
      SHELL: fish
  - name: fish
    command: /usr/bin/fish -f .dotfiles/fish/config.fish
    env:
      EDITOR: vim
      AWS_PROFILE: private