image: moshedayan/gitpod:latest

tasks:
  - name: tmux
    command: /usr/bin/tmux -f .dotfiles/.tmux.conf
    env:
      EDITOR: vim
      AWS_PROFILE: private
      SHELL: fish
  - name: fish
    command: /usr/bin/fish -f .dotfiles/config.fish
    env:
      EDITOR: vim
      AWS_PROFILE: private